import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['m.media-amazon.com', 'gratisography.com', 'encrypted-tbn0.gstatic.com', 'static.vecteezy.com'], // Add the external domain here
  },
  /* config options here */
};

export default nextConfig;
